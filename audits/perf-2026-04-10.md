# Performance Audit – 2026-04-10

**Auditor:** Claude (automated scan)
**Scope:** `tennis-club/app/` – alle Screens & Components (90 Files)
**Sortierung:** Impact (Critical → High → Medium)

---

## CRITICAL – FPS-Killer

### 1. Kein FlashList in gesamter Codebase – 20× FlatList stattdessen

**Impact:** ~10-15 FPS Verlust bei Listen >20 Items, fehlende Cell-Recycling
**Betrifft:** ALLE Listen-Screens

FlashList ist nicht einmal als Dependency installiert. Jede FlatList erstellt bei jedem Scroll neue Views statt sie zu recyclen.

**Betroffene Stellen (nach Kritikalität):**

| Datei | Zeile | Kontext | Erwartete Listengröße |
|---|---|---|---|
| `app/channel/[id].tsx` | L117 | Chat-Nachrichten | **unbegrenzt (100+)** |
| `app/ranking/index.tsx` | L262 | Rangliste | **30-100+** |
| `app/notifications/index.tsx` | L150 | Benachrichtigungen | **50-200+** |
| `app/admin/members.tsx` | L121 | Mitgliederliste | **50-300+** |
| `app/(tabs)/channels.tsx` | L53 | Channel-Liste | 10-30 |
| `app/(tabs)/calendar.tsx` | L129 | Termine | 20-50 |
| `app/(tabs)/tournaments.tsx` | L82 | Turniere | 5-20 |
| `app/todo/index.tsx` | L106 | Todos | 10-50 |
| `app/forms.tsx` | L64 | Formulare | 5-20 |
| `app/files.tsx` | L157 | Dateien | 10-50 |
| `src/components/team/KaderSheet.tsx` | L138, L212 | Kader-Listen | 10-30 |
| `src/components/team/DateienTab.tsx` | L160 | Team-Dateien | 10-50 |
| `src/components/team/TeamChatTab.tsx` | L93 | Team-Chat | **unbegrenzt** |
| `src/components/team/TodosTab.tsx` | L50 | Team-Todos | 5-20 |
| `src/components/team/SpieleTab.tsx` | L66 | Spiele (SectionList) | 10-30 |
| `app/(tabs)/teams.tsx` | L156 | Teams (SectionList) | 5-15 |
| `src/components/tournament/PartnerPickerModal.tsx` | L60 | Partner-Auswahl | 10-50 |
| `app/channels-manage.tsx` | L40 | Channel-Verwaltung | 5-20 |
| `app/tournament/[id].tsx` | L231 | Turnier-Detail | 8-32 |

**Fix (Beispiel `channel/[id].tsx`):**

```diff
- import { FlatList } from 'react-native';
+ import { FlashList } from '@shopify/flash-list';

- <FlatList<ChatMessage>
+ <FlashList<ChatMessage>
    inverted
    data={messages}
    keyExtractor={(item) => item.id}
    renderItem={...}
+   estimatedItemSize={80}
    removeClippedSubviews
-   maxToRenderPerBatch={10}
-   windowSize={5}
  />
```

**Schritt 1:** `cd app && npx expo install @shopify/flash-list`

**Geschätzter Impact:** +10-15 FPS in langen Listen, ~30% weniger RAM bei Chat-Screens

---

### 2. Inline-Funktionen in renderItem – 12 Stellen

**Impact:** Neue Closure pro Render-Zyklus → unnötige Re-Renders aller Items

| Datei | Zeile | Code |
|---|---|---|
| `app/channel/[id].tsx` | L121 | `renderItem={({ item }) => (<>...` (40 Zeilen inline!) |
| `app/(tabs)/calendar.tsx` | L132 | `renderItem={({ item }) => (<AgendaItem .../>)` |
| `app/(tabs)/channels.tsx` | L56 | `renderItem={({ item }) => <ChannelListItem .../>}` |
| `src/components/team/DateienTab.tsx` | L203 | `renderItem={({ item }) => (...)` |
| `src/components/team/SpieleTab.tsx` | L137 | `renderItem={({ item }) => (...)` |
| `src/components/team/TeamChatTab.tsx` | L97 | `renderItem={({ item }) => (...)` |
| `src/components/team/TodosTab.tsx` | L60 | `renderItem={({ item }) => {...}` |
| `src/components/team/KaderSheet.tsx` | L142, L222 | 2× `renderItem={({ item }) => (...)` |
| `app/forms.tsx` | L67 | `renderItem={({ item }) => (...)` |
| `src/components/tournament/PartnerPickerModal.tsx` | L64 | `renderItem={({ item }) => (...)` |
| `app/tournament/[id].tsx` | L235 | `renderItem={({ item }) => (...)` |

**Fix (Beispiel `calendar.tsx`):**

```diff
+ const renderAgendaItem = useCallback(({ item }: { item: CalendarEvent }) => (
+   <AgendaItem
+     event={item}
+     isTrainer={isTrainer}
+     onShowTrainerOverview={() => setTrainerEventId(item.id)}
+   />
+ ), [isTrainer]);

  <FlatList
    data={filteredEvents}
-   renderItem={({ item }) => (
-     <AgendaItem event={item} isTrainer={isTrainer} onShowTrainerOverview={() => setTrainerEventId(item.id)} />
-   )}
+   renderItem={renderAgendaItem}
  />
```

**Geschätzter Impact:** ~5 FPS bei schnellem Scrollen, weniger GC-Pressure

---

### 3. Inline-Styles in Listen-Items – Massive Anzahl

**Impact:** Neues Style-Object pro Frame → GC-Pressure, verhindert PureComponent-Optimierung

**Schlimmste Übeltäter (in renderItem/List-Komponenten):**

| Datei | Zeile(n) | Inline-Style Count |
|---|---|---|
| `app/ranking/index.tsx` | L170-L227 | **10× `style={{...}}`** in renderRanking |
| `app/notifications/index.tsx` | L76-L120 | **6× `style={{...}}`** in renderNotification |
| `app/todo/index.tsx` | L38-L71 | **5× `style={{...}}`** in renderTodo |
| `app/(tabs)/tournaments.tsx` | L40-L48 | **3× `style={{...}}`** in renderTournament (mit pressed!) |
| `app/admin/members.tsx` | L77-L84 | **3× `style={{...}}`** in renderMember |
| `src/components/team/SpieleTab.tsx` | L72-L155 | **7× `style={{...}}`** in SectionList Items |
| `src/components/team/KaderSheet.tsx` | L131-L239 | **4× `style={{...}}`** in FlatList Items |
| `src/components/chat/MessageBubble.tsx` | L102, L211, L258 | **3× `style={{...}}`** (Chat-List-Item!) |

**Fix (Beispiel `ranking/index.tsx:204`):**

```diff
- <View style={{ flex: 1, marginLeft: spacing.md }}>
+ <View style={styles.rankContent}>

  // Am Ende der Datei:
  const styles = StyleSheet.create({
+   rankContent: { flex: 1, marginLeft: 12 },
  });
```

**Hinweis:** Dynamische Werte (Theme-Farben) erfordern `useMemo` oder separate Style-Berechnung.

**Geschätzter Impact:** ~3-5 FPS in langen Listen, ~15% weniger GC-Pauses

---

### 4. Legacy `Animated` API statt Reanimated – 3 Dateien

**Impact:** Animationen auf JS-Thread statt UI-Thread → Jank bei 60fps

| Datei | Zeile | Pattern |
|---|---|---|
| `src/components/ui/LoadingSkeleton.tsx` | L2, L17-38 | `Animated.loop` + `Animated.timing` |
| `src/components/chat/MessageBubble.tsx` | L2, L52-78 | `Animated.Value` + `PanResponder` + `Animated.spring` |

**Hinweis:** `Toast.tsx` nutzt bereits korrekt `react-native-reanimated` (FadeIn/FadeOut).
`Button.tsx` nutzt ebenfalls Reanimated (animatedStyle).

**Fix (`LoadingSkeleton.tsx` – komplett):**

```diff
- import React, { useEffect, useRef } from 'react';
- import { View, StyleSheet, Animated, Easing } from 'react-native';
+ import React from 'react';
+ import { View, StyleSheet } from 'react-native';
+ import Animated, {
+   useSharedValue,
+   useAnimatedStyle,
+   withRepeat,
+   withSequence,
+   withTiming,
+   Easing,
+ } from 'react-native-reanimated';
  import { useTheme } from '../../theme';

  export function LoadingSkeleton({ width, height, borderRadius = 8 }: LoadingSkeletonProps) {
    const { colors } = useTheme();
-   const opacity = useRef(new Animated.Value(0.3)).current;
-
-   useEffect(() => {
-     const animation = Animated.loop(
-       Animated.sequence([
-         Animated.timing(opacity, { toValue: 1, ... }),
-         Animated.timing(opacity, { toValue: 0.3, ... }),
-       ])
-     );
-     animation.start();
-     return () => animation.stop();
-   }, [opacity]);
+   const opacity = useSharedValue(0.3);
+
+   React.useEffect(() => {
+     opacity.value = withRepeat(
+       withSequence(
+         withTiming(1, { duration: 1000, easing: Easing.inOut(Easing.ease) }),
+         withTiming(0.3, { duration: 1000, easing: Easing.inOut(Easing.ease) }),
+       ),
+       -1
+     );
+   }, [opacity]);
+
+   const animatedStyle = useAnimatedStyle(() => ({
+     opacity: opacity.value,
+     backgroundColor: colors.backgroundSecondary,
+     borderRadius,
+   }));

    return (
      <View style={[styles.container, { width, height, borderRadius }]}>
-       <Animated.View style={[styles.shimmer, { backgroundColor: colors.backgroundSecondary, borderRadius, opacity }]} />
+       <Animated.View style={[styles.shimmer, animatedStyle]} />
      </View>
    );
  }
```

**Fix (`MessageBubble.tsx` – Swipe-Reply):**

Migrate `PanResponder` + `Animated` → `react-native-gesture-handler` `Gesture.Pan()` + Reanimated `useSharedValue`/`useAnimatedStyle`. Dies ist ein größeres Refactoring (~50 Zeilen).

**Geschätzter Impact:** +5-8 FPS während Animationen (bewegt Logic vom JS-Thread auf UI-Thread)

---

### 5. Bilder ohne `expo-image` – RN `<Image>` überall

**Impact:** Kein Disk-Caching, kein blurhash, kein progressive loading, kein Memory-Management

`expo-image` ist nicht installiert – alle 14 `<Image>`-Nutzungen verwenden die langsame RN-Variante:

| Datei | Zeile | Kontext |
|---|---|---|
| `src/components/ui/Avatar.tsx` | L34 | Avatar-Bilder (häufigste Komponente!) |
| `src/components/chat/MessageBubble.tsx` | L106, L127 | Chat-Media |
| `src/components/home/NewsFeed.tsx` | L79 | News-Bilder |
| `src/components/match/PhotoGrid.tsx` | L32 | Match-Fotos |
| `app/channel/[id].tsx` | L204 | Image Viewer |
| `app/news/[id].tsx` | L94 | News Detail |
| `app/files.tsx` | L193 | File Preview |
| `src/components/team/ImageViewerModal.tsx` | L28 | Image Viewer |
| `src/components/team/FileUploadModal.tsx` | L84 | Upload Preview |
| `src/components/forms/CourtDamageForm.tsx` | L178 | Form Preview |
| `src/components/forms/MediaUploadForm.tsx` | L81 | Upload Preview |
| `src/components/home/HeroHeader.tsx` | L139 | Hero Background |

**Fix:**

```diff
- import { Image } from 'react-native';
+ import { Image } from 'expo-image';

- <Image source={{ uri: url }} style={styles.image} resizeMode="cover" />
+ <Image source={{ uri: url }} style={styles.image} contentFit="cover" />
```

**Schritt 1:** `cd app && npx expo install expo-image`

**Geschätzter Impact:** ~50-80% weniger Bild-Memory, ~200ms schnellere Bildanzeige, Disk-Cache

---

### 6. `PhotoGrid.tsx:11` – `Dimensions.get()` bei jedem Render

**Datei:** [PhotoGrid.tsx:11](app/src/components/match/PhotoGrid.tsx#L11)

```typescript
const imageWidth = (Dimensions.get('window').width - spacing.xl * 2 - spacing.sm) / 2;
```

Wird bei JEDEM Render aufgerufen, verursacht Bridge-Call.

**Fix:**

```diff
+ import { useWindowDimensions } from 'react-native';

  export function PhotoGrid({ photos }: PhotoGridProps) {
-   const imageWidth = (Dimensions.get('window').width - spacing.xl * 2 - spacing.sm) / 2;
+   const { width } = useWindowDimensions();
+   const imageWidth = useMemo(() => (width - spacing.xl * 2 - spacing.sm) / 2, [width, spacing]);
```

**Geschätzter Impact:** ~1-2ms pro Render gespart, reagiert auf Rotation

---

## HIGH

### 7. Fehlender `ItemSeparatorComponent` Memo – `channels.tsx:32`

**Datei:** [channels.tsx:32-40](app/app/(tabs)/channels.tsx#L32-L40)

```typescript
const separator = () => (
  <View style={{ height: StyleSheet.hairlineWidth, backgroundColor: colors.separator, marginLeft: 86 }} />
);
```

Inline-Funktion + Inline-Style → neue Closure + neues Object bei jedem Render.

**Fix:**

```diff
+ const Separator = React.memo(() => {
+   const { colors } = useTheme();
+   return <View style={[styles.separator, { backgroundColor: colors.separator }]} />;
+ });

  // In StyleSheet:
+ separator: { height: StyleSheet.hairlineWidth, marginLeft: 86 },
```

**Geschätzter Impact:** Minimal allein, aber Teil des Gesamtbildes bei häufigen Re-Renders

---

### 8. Kein `InteractionManager` – Heavy Screens laden sofort

Kein einziger `InteractionManager.runAfterInteractions`-Aufruf in der gesamten App. Screens wie `ranking/index.tsx`, `channel/[id].tsx` und `calendar.tsx` laden sofort heavy content inkl. Kalender-Widget.

**Fix (Beispiel für Calendar Screen):**

```diff
+ import { InteractionManager } from 'react-native';

  const [ready, setReady] = useState(false);

  useEffect(() => {
    const task = InteractionManager.runAfterInteractions(() => setReady(true));
    return () => task.cancel();
  }, []);

  if (!ready) return <LoadingSkeleton ... />;
```

**Geschätzter Impact:** ~50-100ms schnellere Navigation-Transition, weniger Jank bei Screen-Wechsel

---

### 9. `ToastContext` Provider-Value ohne Memo – `Toast.tsx:43`

**Datei:** [Toast.tsx:43](app/src/components/ui/Toast.tsx#L43)

```typescript
<ToastContext.Provider value={{ showToast }}>
```

`{ showToast }` erzeugt bei jedem Render ein neues Object → alle Consumer re-rendern.

**Fix:**

```diff
+ const contextValue = useMemo(() => ({ showToast }), [showToast]);

- <ToastContext.Provider value={{ showToast }}>
+ <ToastContext.Provider value={contextValue}>
```

**Geschätzter Impact:** Verhindert unnötige Re-Renders des gesamten Component-Trees bei jedem Toast

---

### 10. `notifications` Array nicht memoized – `notifications/index.tsx:65`

**Datei:** [notifications/index.tsx:65](app/app/notifications/index.tsx#L65)

```typescript
const notifications = (data ?? []) as NotificationItem[];
```

Erzeugt bei jedem Render ein neues Array (wenn `data` undefined → neues `[]`).

**Fix:**

```diff
- const notifications = (data ?? []) as NotificationItem[];
+ const notifications = useMemo(() => (data ?? []) as NotificationItem[], [data]);
```

Gleicher Fehler auch in:
- `app/todo/index.tsx:31` – `const todos = (data ?? []) as TodoItem[];`
- `app/(tabs)/tournaments.tsx:83` – `(tournaments ?? []) as Tournament[]` (direkt in JSX!)

**Geschätzter Impact:** Verhindert unnötige FlatList-Reconciliation bei unveränderten Daten

---

### 11. `renderChallengeBanner` nicht memoized – `ranking/index.tsx:96`

**Datei:** [ranking/index.tsx:96-158](app/app/ranking/index.tsx#L96-L158)

60-Zeilen Render-Funktion die bei jedem Render neu erstellt wird und als `ListHeaderComponent` übergeben wird. Enthält zusätzlich 8× Inline-Styles.

**Fix:** `useCallback` + Inline-Styles extrahieren.

**Geschätzter Impact:** ~2-3ms pro Re-Render gespart

---

## MEDIUM

### 12. date-fns – korrekte Tree-Shakable Imports ✅

Alle `date-fns` Imports sind named imports (`import { format } from 'date-fns'`). date-fns v3+ ist tree-shakable bei named imports. **Kein Problem.**

---

### 13. `react-native-calendars` Widget in FlatList ListHeader – `calendar.tsx:143`

**Datei:** [calendar.tsx:143-191](app/app/(tabs)/calendar.tsx#L143-L191)

Das gesamte Calendar-Widget (50 Zeilen JSX) wird als `ListHeaderComponent` inline übergeben → bei jedem FlatList-Render neu erstellt.

**Fix:**

```diff
+ const CalendarHeader = useMemo(() => (
+   <View style={{ paddingHorizontal: spacing.xl, marginBottom: spacing.lg }}>
+     <Calendar ... />
+     ...
+   </View>
+ ), [markedDates, selectedDate, colors, ...]);

- ListHeaderComponent={<View>...</View>}
+ ListHeaderComponent={CalendarHeader}
```

**Geschätzter Impact:** ~5-10ms pro Scroll-Event (Calendar ist teuer zu rendern)

---

### 14. `handleChannelPress` nicht memoized – `channels.tsx:30`

**Datei:** [channels.tsx:30](app/app/(tabs)/channels.tsx#L30)

```typescript
const handleChannelPress = (id: string) => router.push(`/channel/${id}`);
```

Wird bei jedem Render neu erstellt und an jedes ListItem weitergegeben.

**Fix:**

```diff
- const handleChannelPress = (id: string) => router.push(`/channel/${id}`);
+ const handleChannelPress = useCallback((id: string) => router.push(`/channel/${id}`), [router]);
```

Gleicher Fehler auch in:
- `app/notifications/index.tsx:67` – `handleNotificationPress`

**Geschätzter Impact:** Minimal isoliert, relevant in Kombination mit React.memo'd List-Items

---

### 15. `canChallenge` Funktion in Render – `ranking/index.tsx:68`

**Datei:** [ranking/index.tsx:68-78](app/app/ranking/index.tsx#L68-L78)

Wird für jedes Item in `renderRanking` aufgerufen und durchläuft `challenges.some()` jedes Mal.

**Fix:**

```diff
+ const canChallenge = useCallback((item: RankingItem): boolean => {
    ...
+ }, [myRank, user, challenges]);
```

**Geschätzter Impact:** Minimal bei <100 Items, relevant bei wachsender Rangliste

---

### 16. `Dimensions.get()` vs `useWindowDimensions` – `PhotoGrid.tsx`

Siehe #6 oben (dort als Critical eingestuft wegen häufiger Nutzung in Listen).

---

### 17. Keine `React.memo` auf List-Item-Komponenten

Keine der List-Item-Komponenten (`ChannelListItem`, `AgendaItem`, `MessageBubble`, etc.) ist mit `React.memo` gewrapped. In Kombination mit FlashList wäre dies besonders wirkungsvoll.

**Betroffene Komponenten:**
- `ChannelListItem.tsx`
- `AgendaItem.tsx`
- `MessageBubble.tsx`
- `NewsCard` (in `NewsFeed.tsx:40`)
- `FileRow.tsx`
- `FolderRow.tsx`

**Fix:**

```diff
- export function MessageBubble({ ... }: MessageBubbleProps) {
+ export const MessageBubble = React.memo(function MessageBubble({ ... }: MessageBubbleProps) {
    ...
- }
+ });
```

**Geschätzter Impact:** ~20% weniger unnötige Re-Renders in Listen

---

## Zusammenfassung

| # | Befund | Impact | Aufwand |
|---|---|---|---|
| 1 | **Kein FlashList – 20× FlatList** | 🔴 CRITICAL | 2h (install + migrate alle Listen) |
| 2 | **12× Inline renderItem** | 🔴 CRITICAL | 1h |
| 3 | **Massive Inline-Styles in List-Items** | 🔴 CRITICAL | 3h |
| 4 | **Legacy Animated statt Reanimated** | 🔴 CRITICAL | 2h |
| 5 | **RN Image statt expo-image** | 🔴 CRITICAL | 1.5h |
| 6 | **Dimensions.get() in Render** | 🟡 HIGH | 10min |
| 7 | **Separator nicht memoized** | 🟡 HIGH | 10min |
| 8 | **Kein InteractionManager** | 🟡 HIGH | 1h |
| 9 | **Context Provider Value unmemoized** | 🟡 HIGH | 5min |
| 10 | **Arrays nicht memoized** | 🟡 HIGH | 15min |
| 11 | **renderChallengeBanner unmemoized** | 🟡 HIGH | 15min |
| 12 | **date-fns Imports** | ✅ OK | – |
| 13 | **Calendar in ListHeader inline** | 🟠 MEDIUM | 15min |
| 14 | **Handler nicht memoized** | 🟠 MEDIUM | 15min |
| 15 | **canChallenge in Render** | 🟠 MEDIUM | 10min |
| 16 | **Dimensions.get statt Hook** | 🟠 MEDIUM | 10min |
| 17 | **Keine React.memo auf List-Items** | 🟠 MEDIUM | 1h |

### Empfohlene Reihenfolge:
1. **FlashList installieren + migrieren** (größter Einzelimpact)
2. **expo-image installieren + migrieren** (Memory + UX)
3. **Reanimated-Migration** (LoadingSkeleton + MessageBubble)
4. **Inline renderItem → useCallback** (Quick Wins)
5. **Inline-Styles in Listen-Items → StyleSheet** (größter Aufwand, aber nötig)
6. **React.memo auf List-Items** (verstärkt FlashList-Effekt)
7. **Alle kleinen Fixes** (Context, Arrays, InteractionManager)

**Geschätzter Gesamtimpact:**
- **FPS:** +15-25 FPS in Listen-Screens
- **Memory:** ~40-60% weniger RAM für Bilder
- **Startup:** ~100-200ms schnellere Screen-Transitions
- **Bundle:** Minimal (FlashList + expo-image ~50KB gzipped)
